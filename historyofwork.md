# ClawBot — Work History

| Date | Chunk | What We Did | Status |
|------|-------|-------------|--------|
| 2026-02-28 | Chunk 3: Shared Types | Created `shared/types/` with 6 type modules (approvals, cards, monitoring, tasks, gateway, index), tsconfig, and root tsconfig reference. Pure TS types, zero runtime code, zero deps. All skill doc fields covered. | Done — compiles clean |
| 2026-02-28 | Chunk 3 Patch: Spec Alignment | Fixed 8 spec mismatches: added `id: string` to all 4 card types; `PickCard.impliedOdds` → `number`; `TaskStatus` union replaced with spec values (planning/searching/waiting_input/etc.); `ThinkingStep.status` literals → done/error; `TaskUpdate.status` now required; `ApprovalPolicy.requiresApproval` → string union (always/configurable/never); `AuditEntry.decision` inlined with `auto_allowed`; `WatchlistItem.type` → literal union. | Done — compiles clean |
| 2026-02-28 | Chunk 5: Message Router + Test Client | Created `server/gateway/src/message-router.ts` (MessageRouter class with Zod validation, request routing for chat.send/chat.history/approval.resolve/task.stop, event handling with operator broadcast, idempotency checks, findAgentNode helper, approval hook passthrough) and `server/gateway/test-client.ts` (standalone CLI script with operator/node modes, simulated agent streaming responses). Router uses `text` field per spec. `tsc --noEmit` passes clean. | Done — compiles clean |
| 2026-02-28 | Chunk 4: Gateway Foundation | Created `server/gateway/` — the WebSocket control plane (no message router). Files: `package.json` (ESM, ws/uuid/zod deps), `tsconfig.json` (strict, NodeNext, composite, refs shared/types), `src/types.ts` (re-exports all shared types + gateway-local: ConnectedClient, Session, ConnectPayload, DisconnectedClient, MessageHistoryEntry, ApprovalHookFn, LogEntry), `src/session-manager.ts` (SessionManager class: session CRUD, history capped 1000, idempotency dedup, 30s reconnect window, 10s periodic cleanup), `src/ws-server.ts` (GatewayServer class: WS+HTTP server, 10s handshake timeout, Zod-validated connect flow, `/health` endpoint, `setMessageHandler()` plug point for router, agent disconnect notifications to operators), `src/index.ts` (entry point, env port default 8080, graceful SIGINT/SIGTERM shutdown). Root `tsconfig.json` updated with gateway reference. | Done — compiles clean, server starts, `/health` returns valid JSON |
| 2026-02-28 | Chunk 6: Gateway Integration | Wired MessageRouter into GatewayServer. Added `import MessageRouter` and `this.setMessageHandler((client, raw) => router.handleMessage(client, raw))` in `ws-server.ts` constructor. Two-line change. Verified: `tsc -b` zero errors, server starts, operator connects + gets sessionId, agent node joins same session, chat.send routes through router (returns NO_AGENT when agent not connected, routes to agent when connected), chat.history returns stored messages, `/health` reports connections/sessions. Full gateway pipeline operational. | Done — compiles clean, end-to-end tested |
| 2026-02-28 | Chunk 7: Approval Policy Engine | Created `server/gateway/approvals/` — self-contained policy rules engine. Files: `approvals.config.json` (8 policy entries: 5 safety-critical as `always`, search/read as `never`, monitor as `configurable` with 3-item allowlist), `policy-engine.ts` (PolicyEngine class: `ALWAYS_ASK` hardcoded Set for 5 safety-critical actions that can't be overridden by config, `loadPolicies()` reads JSON config with fallback to hardcoded defaults, `checkPolicy(action, context)` 5-step evaluation — ALWAYS_ASK override → unknown action default → never/always/configurable with allowlist, plus `reloadPolicies()`/`getPolicy()`/`listPolicies()` utilities), `__tests__/policy-engine.test.ts` (node:test + node:assert, 12 test cases covering all ALWAYS_ASK actions, never actions, configurable allowlist hit/miss/missing target, unknown action safe-by-default, reason string presence). Lives outside `src/`, run via `npx tsx`. | Done — 12/12 tests pass |
| 2026-02-28 | Chunk 8: Audit Trail Module | Created `server/gateway/approvals/audit-log.ts` — companion to PolicyEngine that records every action check. Exports `AuditLog` class with in-memory `AuditEntry[]` storage (TODO for persistent store). Methods: `log(entry)` generates uuid + ISO timestamp, pushes to array, emits structured JSON to console, returns full `AuditEntry`; `getLog(taskId?)` filters by taskId or returns shallow copy; `getEntry(id)` find by id; `getByAction(action)` and `getByDecision(decision)` filter helpers; `clear()` resets for testing; `size()` returns count; `summary()` returns `{ total, approved, denied, autoAllowed }` breakdown. Imports `AuditEntry` from `shared/types/index.js` (action typed as `ApprovalAction`), uses `uuid` v9. Lives alongside `policy-engine.ts` outside `src/`. | Done — compiles clean |
| 2026-02-28 | Chunk 9: Task Control Module | Created `server/gateway/approvals/task-control.ts` — thin coordination layer for stop/halt commands. Exports `TaskControl` class. Constructor takes `ApprovalManager` and `GatewayEmitFn` (imported from `./approval-manager.js` — not yet built). `stopTask(taskId, sessionId)` cancels pending approvals via `approvalManager.cancelForTask()`, emits `agent/stream:lifecycle` (status: end, reason: user_stopped) and `task/update` (status: stopped), logs structured JSON, returns `{ cancelledApprovals }`. `stopMonitoring(watchlistItemId, sessionId)` emits `monitoring/stopped` event and logs. Verified with temporary type stub. | Done — compiles clean |
| 2026-02-28 | Chunk 10: Approval Lifecycle Manager | Created `server/gateway/approvals/approval-manager.ts` — async approval lifecycle manager. Exports `ApprovalManager` class + `GatewayEmitFn` type. Constructor takes `gatewayEmit` callback and configurable `timeoutMs` (default 10 min). Core pattern: `createApproval(taskId, action, description, details, sessionId)` returns a `Promise<ApprovalDecision>` that blocks until `resolve()` is called or timeout fires (auto-denies). Promise resolve function captured in `PendingApproval` entry. `resolve(approvalId, decision)` clears timeout, moves pending→resolved, unblocks Promise, emits `approval/resolved`. `cancelForTask(taskId)` auto-denies all pending for a task. Also: `listPending(sessionId?)`, `getPending()`, `getResolved()`, `pendingCount()`, `destroy()`. Emits gateway events: `approval/requested`, `approval/resolved`, `approval/timeout`. Uses shared types (`ApprovalAction`, `ApprovalDecision`, `ApprovalRequest`, `ApprovalResponse`) from `shared/types/index.js`. In-memory Maps with TODO for persistent store. | Done — 6/6 functional tests pass |
| 2026-02-28 | Session C: Message Store | Created `ios/ClawBot/Chat/MessageStore.swift` — actor-based message persistence. `actor MessageStore` with two inits (default `Documents/ClawBotMessages/` + testable `init(directory:)`). 6 methods: `save(_:sessionId:)` filters out `isStreaming` messages then writes atomically (`[.atomic]`); `load(sessionId:)` returns `[]` for missing files; `delete(sessionId:)`; `listSessions()` derives IDs from `.json` filenames; `totalSize()` sums bytes across all session files; `clearAll()` removes entire directory. ISO 8601 date encoding, sorted-keys JSON output. Session IDs sanitized (`/` and `\` → `_`) for filename safety. Depends on `ChatMessage` from `ios/ClawBot/Models/ChatMessage.swift` (not modified). | Done — compiles clean (`swiftc -typecheck` exit 0) |
| 2026-02-28 | Session D: WebSocket Service iOS | Created `ios/ClawBot/Chat/WebSocketService.swift` — iOS WebSocket client (383 lines). Contains: `ConnectionState` enum (disconnected/connecting/connected/reconnecting), `WebSocketServiceProtocol` (6 methods + 3 Combine publishers + state property for mockability), `WebSocketService: ObservableObject` (real impl using URLSessionWebSocketTask + Combine subjects, `forceSend()` for handshake bypass, `handleReceived()` extracts deviceToken/sessionId from connect response and publishes via Combine, exponential backoff reconnect: `min(pow(2.0, attempts), 30.0)` → 1,2,4,8,16,30s capped), `MockWebSocketService` (preview/test stub with `simulateMessage/StreamEvent/StateChange` helpers + recorded `sentMessages` array). Handshake sends `role: "operator"`, `scopes: ["chat","approval","task"]`, optional `deviceToken`/`sessionId` on reconnect — matches gateway `ConnectPayloadSchema`. Uses `ObservableObject`+`@Published`+Combine (not `@Observable`) because publisher pattern requires it. All `WSMessage.request()` calls use 3-arg `(method:id:payload:)` signature. | Done — structurally verified |
| 2026-02-28 | Session E: ThinkingStepView | Created `ios/ClawBot/Chat/ThinkingStepView.swift` — thinking step pill/chip UI (single file, 4 components). `ThinkingStepPill`: HStack with status icon + description text, 4 status variants — `.pending` (circle.dotted, tertiary color, systemGray6 bg), `.running` (ProgressView mini, systemGray5 bg), `.done` (checkmark.circle.fill green, systemGray6 bg), `.error` (exclamationmark.circle.fill red, red 10% bg). Capsule-clipped with 10pt h-padding / 6pt v-padding. `ThinkingStepsContainer`: wraps pills in custom `FlowLayout`, `isVisible` bool triggers 150ms easeOut fade animation, 8pt vertical + horizontal padding. `FlowLayout`: custom `Layout` protocol conformance — `sizeThatFits` + `placeSubviews` compute row-break positions when children exceed available width, configurable 6pt default spacing. 3 `#Preview` blocks: mixed running states, single error, faded out. All ThinkingStep inits include `timestamp: ""` to match model's required parameter. Informed by chowder-iOS reference patterns (ThinkingShimmerView.swift, ActivityStepRow.swift) but follows ClawBot's own spec. | Done — compiles clean (`swiftc -typecheck` exit 0) |
| 2026-02-28 | Session E: ChatViewModel + ChatView + App Entry | Created 4 files wiring all iOS components into a functioning chat UI. `ios/ClawBot/Chat/ChatViewModel.swift` — `@MainActor ObservableObject`, subscribes to `statePublisher`/`streamEventPublisher` via Combine, handles all 5 StreamEvent cases (assistantDelta appends to streaming message, lifecycle start/end manages placeholder + persistence + thinking fade, stateDelta creates ThinkingSteps with `id: UUID().uuidString` + `timestamp: ISO8601DateFormatter().string(from: Date())`, taskUpdate upserts steps by id), `sendMessage()` guards empty/streaming then calls synchronous `sendChatMessage(text:)`, persists via `messageStore.save(msgs, sessionId: id)` (unlabeled first param). `ios/ClawBot/Chat/ChatView.swift` — `@StateObject` ChatViewModel, ScrollViewReader auto-scroll on message count change, MessageBubble (blue right-aligned user / gray left-aligned assistant with streaming ProgressView), input bar disabled during streaming/disconnected, connection indicator dot in toolbar (green=connected, yellow=connecting/reconnecting, red=disconnected), empty state with icon + prompt. `ios/ClawBot/App/ClawBotApp.swift` — `@main` entry, `WindowGroup { ContentView() }`. `ios/ClawBot/App/ContentView.swift` — TabView with Chat (live), Tasks (placeholder), Settings (placeholder). Fixed 6 spec→actual API mismatches: publisher names, connect signature, sync send, string lifecycle status, ThinkingStep init fields, unlabeled save param. | Done — `swiftc -typecheck` all 11 Swift files, zero errors |
| 2026-02-28 | Session F: Chowder UI Patterns | Ported polished chat UI from chowder-iOS — 4 new files, 2 modified. **New:** `ios/ClawBot/Chat/MarkdownContentView.swift` (verbatim from chowder: block parser for code/headings/lists/blockquotes, inline markdown via AttributedString, cached parsing in init), `ios/ClawBot/Chat/ThinkingShimmerView.swift` (verbatim from chowder: pulsing dot + shimmer gradient, 1.5s easeInOut repeating animation), `ios/ClawBot/Chat/ChatHeaderView.swift` (simplified port: blue initials avatar, bot name, online/offline dot+text, material blur background; removed avatar image loading, taskSummary, settings/debug buttons), `ios/ClawBot/Chat/MessageBubbleView.swift` (adapted: uses ChatMessage not Message, assistant=MarkdownContentView with textSelection, user=plain Text in blue rect, ProgressView for empty streaming, copy context menu). **Modified:** `ChatViewModel.swift` — added `@Published var currentShimmerLabel: String? = nil`, set "Thinking..." on lifecycle start, clear on lifecycle end, update to summary on stateDelta, clear on first text delta. `ChatView.swift` — major overhaul: removed NavigationStack/toolbar/inline MessageBubble, added ChatHeaderView overlay with 72pt spacer, MessageBubbleView in ForEach, thinking steps + shimmer inline in LazyVStack, scroll-to-bottom FAB with isAtBottom tracking via onAppear/onDisappear on bottom anchor, capsule-styled input bar (32pt send button), scrollDismissesKeyboard(.interactively), scenePhase reconnect on foreground return. 15 Swift files total. | Done — `swiftc -typecheck` all 15 files, zero errors |
| 2026-02-28 | Session I: HouseCardView + DocCardView | Created 2 card view files in `ios/ClawBot/Cards/`. **`HouseCardView.swift`** — full apartment listing card: rent+bedrooms header (bedroom count as blue capsule badge), address, area, commute chip (mode→SF Symbol mapping via `commuteIcon` computed property: transit→tram.fill, driving→car.fill, walking→figure.walk, biking→bicycle), move-in date with calendar icon, red flags section (FlowLayout pills with warning triangles, conditional on `!redFlags.isEmpty`), collapsible required docs (DisclosureGroup with `@State showDocs`), lease terms, "View Listing on {source}" button via `@Environment(\.openURL)`. **`DocCardView.swift`** — horizontal doc link card: icon box (44x44, `iconColor`→SwiftUI Color mapping for blue/green/purple/orange/gray), title+preview text (line-limited), relative date via `RelativeDateTimeFormatter` (.short units style), open button with arrow.up.right.square icon. Both match FlightCardView card styling (systemBackground, 12pt corners, shadow 0.08 opacity). **Model API mismatches fixed from spec:** `commute.formatted` → inline `"\(time) \(mode) to \(destination)"` (Commute has no `formatted` property); `.mock`/`.mockWithFlags` → `.mockClean`/`.mockRedFlags`; `.mock`/`.mockSheet` → `.mockDocument`/`.mockSpreadsheet`; removed duplicate FlowLayout (reuses existing internal struct from ThinkingStepView.swift). 23 Swift files total. | Done — `swiftc -typecheck` all 23 files, zero errors; `tsc -b` zero errors |
| 2026-02-28 | Session J: CardListView + CardComparisonView | Created 2 container view files in `ios/ClawBot/Cards/`. **`CardListView.swift`** — two structs: `CardListView` (ScrollView+LazyVStack, `cardView(for:)` switch dispatches AnyCard cases to FlightCardView/HouseCardView/PickCardView/DocCardView, empty state with `rectangle.on.rectangle.slash` icon + headline + subheadline) and `CardListWithSwipeView` (List variant with `.listRowSeparator(.hidden)`, `.swipeActions(edge: .leading)` Save bookmark blue, `.swipeActions(edge: .trailing)` Dismiss xmark destructive). Both take `cards: [AnyCard]`, optional `onSave`/`onDismiss` closures. 3 previews. **`CardComparisonView.swift`** — paging comparison: header "Compare (N)" + "X of N" counter, `TabView(selection: $currentPage)` with `.tabViewStyle(.page(indexDisplayMode: .automatic))`, computed `cardHeight` per card type (flight→320, house→480, pick→300, doc→250). Two conditional comparison bars: **flight bar** (shown when all cards are `.flight`) finds cheapest by `price.amount` + fastest by `duration` string, **house bar** (shown when all cards are `.house`) finds cheapest by `rent.amount` + fewest flags by `redFlags.count`. Private `ComparisonChip` struct (icon + label + value with highlight color tinted background). Uses `exclamationmark.shield` SF Symbol (not nonexistent `shield.checkered`). Correct mock names throughout: `.mockDefault`, `.mockCheapest`, `.mockFastest`, `.mockClean`, `.mockRedFlags`, `.mockFavorable`, `.mockDocument`, `.mockSpreadsheet`. 3 previews. 27 Swift files total. | Done — `swiftc -typecheck` all 27 files, zero errors; `tsc -b` zero errors |
| 2026-02-28 | Re-verification: CardListView + CardComparisonView | Re-ran `swiftc -typecheck` on all 14 card-related Swift files (models + views). Confirmed CardListView.swift and CardComparisonView.swift still compile with zero errors. No code changes made — existing implementation already satisfies all spec requirements. 5 known spec bugs (mock names, SF Symbol `shield.checkered`, `.swipeActions` on LazyVStack) were already fixed in Session J. | Done — zero errors, no changes needed |
| 2026-02-28 | Session J: TaskFeedView + TaskRowView | Created 2 task feed view files in `ios/ClawBot/TaskFeed/`. **`TaskRowView.swift`** — `TaskRowView` struct: HStack with status dot (color-coded: blue=planning/searching/executing, orange=waitingInput/waitingApproval, purple=monitoring, green=completed, gray=stopped), goal text (`.subheadline.medium`, 2-line limit), inner HStack with `StatusBadge` + card count chip (`rectangle.stack` icon, conditional on non-empty cards) + `Spacer` + relative timestamp. Linear `ProgressView` shown when `task.status.isActive`. `StatusBadge` struct: colored capsule pill showing `status.displayName` with same color mapping as dot. Private `relativeTime(_:)` helper using `RelativeDateTimeFormatter` (`.abbreviated` units style). 4 `#Preview` macros (completed, searching, awaiting approval, stopped). **`TaskFeedView.swift`** — `TaskFeedView` struct: `NavigationStack` + `List`, accepts `tasks: [AgentTask]`, optional `onRefresh: (() async -> Void)?`, optional `onSelectTask: ((AgentTask) -> Void)?`. `sortedTasks` computed: active first (`isActive` true before false), then reverse chronological by `updatedAt` string comparison. Empty state: clipboard icon + "No tasks yet" + "Start a conversation to create one." Pull-to-refresh via `.refreshable`. `.listStyle(.plain)`, `.contentShape(Rectangle())` + `.onTapGesture` for row selection. 2 `#Preview` macros (with items, empty). Both files depend on `TaskModels.swift` (AgentTask, TaskStatus) which is being built in a separate session — expected API: `.isActive`, `.displayName`, static mocks `.mock`/`.mockSearching`/`.mockAwaitingApproval`/`.mockStopped`. `ContentView.swift` not modified (still shows `TasksPlaceholder()`). 27 Swift files total. | Done — `swiftc -typecheck` all 27 files with temporary TaskModels stub, zero errors |
| 2026-02-28 | Session C: WatchlistView + WatchlistDetailView + AlertCardView | Created 3 Watchlist view files in `ios/ClawBot/TaskFeed/`. **`AlertCardView.swift`** — compact alert card: Button with HStack (adaptive icon based on message content keywords: price→arrow.down.circle.fill, listing/new→house.badge.plus, line/moved→chart.line.uptrend.xyaxis, fallback→bell.badge.fill), message text (2-line limit), relative timestamp, chevron. Adaptive color (green for drops/favorable, blue for new, orange default). `.plain` buttonStyle, 10pt rounded rect, shadow. 3 previews. **`WatchlistView.swift`** — two structs: `WatchlistView` (NavigationStack, List with "Recent Alerts" section showing top 3 sorted by timestamp + "Active Watches" section with WatchlistRowView per item, `.insetGrouped` list style, empty state with eye.slash icon) and `WatchlistRowView` (type icon colored by WatchlistType, description, metadata chips — type badge capsule, interval, last checked, alert count — and Toggle for active state with `@State private var isActive` initialized from `item.active`, `onChange` callback). 2 previews. **`WatchlistDetailView.swift`** — List with 3 sections: Details (LabeledContent rows for type/interval/lastChecked/status), Filters (sorted key-value pairs), Alerts (filtered to matching watchlistItemId, sorted reverse chronological, data chips in capsules, relative time). 2 previews. All 3 files depend on MonitoringModels.swift (WatchlistItem, WatchlistType, MonitoringAlert) which is being built in a separate session. 30 Swift files total. | Done — files created, pending MonitoringModels.swift for compilation |
| 2026-02-28 | Session L: TaskDetailView | Created `ios/ClawBot/TaskFeed/TaskDetailView.swift` — task detail screen with 6 sections: header (StatusBadge + goal + active ProgressView), steps timeline (ForEach with `stepIcon(for:)` ViewBuilder — dotted circle/ProgressView/checkmark/xmark per ThinkingStepStatus, connector lines between steps, toolName capsule chips, result text), pending approvals (filtered by task.id, orange-tinted cards with approve/deny buttons), cards stub (count placeholder for future CardListView integration), timestamps (ISO 8601 → medium date + short time via `formatDate(_:)`), stop button (confirmation dialog with destructive action). 4 `#Preview` blocks (completed, searching, awaiting approval, stopped). Dependencies: StatusBadge (TaskRowView.swift), AgentTask/TaskStatus/PendingApproval (TaskModels.swift), ThinkingStepStatus (ThinkingStep.swift). 35 Swift files total. | Done — `swiftc -typecheck` all 35 files, zero errors |
| 2026-02-28 | Session K: TaskModels + MonitoringModels + Cross-Check | Created 2 model files in `ios/ClawBot/Models/`. **`TaskModels.swift`** — `TaskStatus` enum (8 cases: planning/searching/executing/waitingInput/waitingApproval/monitoring/completed/stopped, raw `String`, CaseIterable) with computed `displayName` (human-readable) and `isActive` (true for all except completed/stopped). `AgentTask` struct (Identifiable, Codable, Equatable, Hashable) with `cardIds: [String]` and `approvalIds: [String]` (ID-only references, full objects fetched on demand), computed `createdDate`/`updatedDate`/`completedStepCount`. `PendingApproval` struct (Identifiable, Codable, Equatable). 4 static `AgentTask` mocks (`.mock`/`.mockSearching`/`.mockAwaitingApproval`/`.mockStopped`) + 1 `PendingApproval` mock. **`MonitoringModels.swift`** — `WatchlistType` enum (3 cases: price/listing/line) with computed `displayName`/`iconName` (SF Symbols). `WatchlistItem` struct with `filters: [String: String]`, computed `intervalFormatted` (seconds→human readable) and `lastCheckedFormatted` (RelativeDateTimeFormatter). `MonitoringAlert` struct with computed `date`. 3 static `WatchlistItem` mocks + 3 `MonitoringAlert` mocks. **Fixed:** `TaskRowView.swift` — `task.cards` → `task.cardIds` (lines 58-59). `ContentView.swift` — `TasksPlaceholder()` → `TaskFeedView(tasks: [])`. 36 Swift files total. | Done — `swiftc -typecheck` all 36 files, zero errors |
| 2026-02-28 | Session M: TaskFeedViewModel + ContentView Full Wiring | Created 1 new file, modified 3 existing files. **New:** `ios/ClawBot/TaskFeed/TaskFeedViewModel.swift` — `@MainActor ObservableObject`, holds `@Published` tasks/watchItems/alerts/pendingApprovals/selectedTask/selectedWatchItem. Subscribes to `streamEventPublisher` via Combine. `handleEvent` processes `.taskUpdate` (status + step upsert) and `.approvalRequested` (append PendingApproval). Actions: `refresh()`, `stopTask()` (optimistic update + WS `sendTaskStop`), `toggleWatchActive()` (optimistic + WS), `approveAction()`/`denyAction()` (remove from pending + WS `sendApprovalResponse`). `loadMockData()` populates all arrays for MVP. **Modified:** `ContentView.swift` — major rewrite: `@StateObject TaskFeedViewModel`, 4 extracted tab computed properties (`tasksTab`/`watchlistsTab`/`settingsTab`), `tasksTab` wraps `TaskFeedView` in `NavigationStack` with `.navigationDestination(item:)` to `TaskDetailView`, `watchlistsTab` wraps `WatchlistView` with `.navigationDestination(item:)` to `WatchlistDetailView`, `.badge(alertBadgeCount)` on watchlists tab. Removed `TasksPlaceholder`/`SettingsPlaceholder` structs. `TaskFeedView.swift` — removed own `NavigationStack` (parent provides it), previews wrap in `NavigationStack` for standalone preview. `WebSocketService.swift` — added `sendApprovalResponse(approvalId:decision:)` and `sendTaskStop(taskId:)` to protocol + both implementations (real + mock). 36 Swift files total. | Done — `swiftc -typecheck` all 36 files, zero errors |
| 2026-03-01 | Session M: ApprovalDetailView | Created `ios/ClawBot/ClawBot/ClawBot/Approvals/ApprovalDetailView.swift` — full-screen approval review view. `ApprovalDetailView` struct: takes `ApprovalRequest` + optional `onApprove`/`onDeny` closures. 6 sections: header (56x56 action icon with tinted background + action name + timeAgo + destructive warning badge), description (body text in systemGray6 card), details (sorted key-value pairs with dividers in systemGray6 card), task link, action buttons (state machine: idle→approving/denying→success/error). `ActionState` enum (idle/approving/denying/success/error). Destructive flow: `.pay`/`.delete` trigger `.confirmationDialog` before approving; non-destructive actions approve immediately. Success state auto-dismisses after 1.2s via `DispatchQueue.main.asyncAfter`. Error state shows retry button resetting to idle. `actionColor` maps iconColor strings to SwiftUI Colors (blue/green/purple/red/orange/indigo/gray). 5 `#Preview` blocks (mockPay destructive, mockDelete destructive, mockSend non-destructive, mockSharePersonalInfo, mockSubmit). Fixed from user spec: `.mockPayment`→`.mockPay`, `.mockShareInfo`→`.mockSharePersonalInfo`, added `case "indigo": .indigo` to actionColor. 37 Swift files total. | Done — `swiftc -typecheck` zero errors |
| 2026-03-01 | Session P: MemorySearch BM25 Engine | Created `server/agent/memory/memory_search.py` — BM25 Okapi keyword search engine for the memory system. **MemorySearchResult** dataclass (entry, score, match_type). **MemorySearch** class with multi-signal scoring pipeline: (1) exact key match → 1.0, (2) partial key match → 0.7, (3) BM25 keyword scoring (IDF = log((N-df+0.5)/(df+0.5)+1), TF norm with k1=1.5 b=0.75, normalized to 0-0.6 range), (4) tag match bonus +0.15/tag capped at 0.45, (5) recency boost +0.1 (24h) / +0.05 (7d). `search_as_dicts()` bridge returns `{key, content, relevance_score}` dicts for both protocols. Standalone `tokenize()` function with stop word removal, `bm25_score_single()` for per-document BM25 scoring. Updated `MemoryStore.search()` to delegate to `MemorySearch.search_as_dicts()` replacing the old simple scorer. Created `tests/test_memory_search.py` with 34 test cases: tokenizer (6), BM25 scorer (6), exact key (3), tag boosting (2), BM25 keyword relevance (4), recency (1), top_k (3), edge cases (5), search_as_dicts (4). Algorithm references: dorianbrown/rank_bm25, xhluca/bm25s, zilliztech/memsearch, openclaw memory docs. Pure Python stdlib (math.log, collections.Counter, re). TODO comments for future: embedding search, vector store, bm25s upgrade, hybrid BM25+vector. | Done — 34/34 tests pass, 133/133 full suite pass |
| 2026-03-01 | Session E: ApprovalsViewModel + Tab Bar Update | Created 1 new file, modified 3 existing files. **New:** `ios/ClawBot/ClawBot/ClawBot/Approvals/ApprovalsViewModel.swift` — `@MainActor final class ObservableObject`. Published: `pending: [ApprovalRequest]`, `history: [ResolvedApproval]`, `selectedApproval: ApprovalRequest?`. Computed: `pendingCount`. Subscribes to `streamEventPublisher` for `.approvalRequested` events — converts `[String: AnyCodable]` details to `[String: String]`, creates `ApprovalRequest`, deduplicates by id, fires local notification via `NotificationManager.sendLocalApprovalNotification(approvalId:title:body:)`, updates badge count. `approve()`/`deny()` call `webSocket.sendApprovalResponse()` then `moveToHistory()` (removes from pending, creates `ApprovalResponse`+`ResolvedApproval`, inserts at history[0], clears badge). Deep link via `NotificationCenter.default.publisher(for: .deepLinkToApproval)` → `navigateToApproval(id:)` sets `selectedApproval`. `Hashable` extension on `ApprovalRequest` (hashes by id) for `.navigationDestination(item:)`. Fixed 6 API mismatches from spec: `streamEventPublisher` not `eventPublisher`, `WSMessage.request(method:id:payload:)` 3-arg, sync `send()`, `final class`, `sendLocalApprovalNotification(approvalId:title:body:)` params, AnyCodable→String conversion. **Modified:** `ApprovalsListView.swift` — removed internal `NavigationStack` wrapper (parent ContentView provides it), previews wrap in `NavigationStack`. `ContentView.swift` — added `@StateObject ApprovalsViewModel`, replaced Settings tab with Approvals tab (`checkmark.shield` icon, `.badge(pendingCount)`), `approvalsTab` computed property wraps `ApprovalsListView` in `NavigationStack` with `.navigationDestination(item:)` to `ApprovalDetailView`. `ClawBotApp.swift` — added `@UIApplicationDelegateAdaptor(AppDelegate.self)`, `NotificationManager.shared.setup()` in `.onAppear`, `AppDelegate` class with `didRegisterForRemoteNotificationsWithDeviceToken`/`didFailToRegisterForRemoteNotificationsWithError` forwarding to NotificationManager. 4 tabs: Chat/Tasks/Approvals/Watchlists. 38 Swift files total. | Done — `xcodebuild build` zero errors (pre-existing PhotoInputHelper warning excluded) |
